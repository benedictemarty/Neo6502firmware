# Backlog — Trinity (firmware de référence Neo6502, bmarty)

Décision bmarty 2026-09-19 : Trinity est **l'unique firmware de référence** ; le fork `bmarty/main` (toolbox, banques,
modes vidéo, Latin-1, R22) est archivé (`archive/bmarty-main-2026-09-19`). Une fonctionnalité n'entre dans Trinity
que validée **sur carte**. Règles : rien d'inventé, petits commits en français (`bmarty <bmarty@mailo.com>`, jamais de
Co-Authored-By), CHANGELOG à chaque modification, tag `trinity-vX.Y.Z` à chaque livraison, UF2 dans `~/neo-carte/`.

| ID | P | Story | État |
|---|---|---|---|
| T-67 | P2 | **La police du mode Hercules est celle du vrai générateur de caractères MDA** (demande bmarty 2026-09-24 : « tu peux faire un dump spécifique au Hercules, pas MDA ? »). Vérification faite, **il n'existe pas de police Hercules distincte** : en mode texte la carte Hercules reprend le générateur de caractères **MDA** d'IBM — c'est en mode graphique qu'elle innove. La police précédente venait de `Lat15-VGA14.psf` (console Linux, domaine public) : bonne hauteur, mais dessin **VGA**, pas MDA. Remplacée par le relevé du générateur MDA publié dans l'**Ultimate Oldschool PC Font Pack v2.2** de VileR (int10h.org), `Bm437_IBM_MDA.otb`, **CC BY-SA 4.0**. Le MDA dessine dans une cellule **9 × 14** dont la 9ᵉ colonne est vide sauf pour les semi-graphiques `$C0-$DF` (colonne répliquée pour joindre les traits) : comme Trinity n'exporte que `$20-$7F`, **aucun glyphe n'est tronqué** et l'espacement d'origine est respecté, notre cellule ayant elle aussi sa 9ᵉ colonne vide. Script `firmware/scripts/mda14.py` (rendu par FreeType à la taille native de la fonte bitmap), source et licence versionnées dans `firmware/scripts/assets/`. 87 des 96 glyphes changent ; coût mémoire nul (flash, `.rodata`). `vga14.py` est conservé comme variante. | **DONE** 0.14.0, à valider carte |
| T-68 | P0 | **Échap au démarrage n'ouvrait plus le menu `boot/`** (retour carte bmarty 2026-09-24, régression de la **0.13.0**). T-66 a introduit un **plancher de 3 s** (`USB_FLOOR`) pendant lequel les logos tiennent l'écran et la console est **muette** : le message `Boot : auto … (Esc = menu, 3 s)` n'apparaît donc que **3 à 4 s après la mise sous tension**, et la fenêtre où `BOOTSelect` scrutait Échap ne commençait qu'à ce moment. On appuie pendant les logos — c'est le geste naturel, et c'est ce que faisait l'ancienne séquence où le message venait tout de suite — donc **trop tôt** : la frappe tombait hors fenêtre. Correction : le gestionnaire clavier **retient** l'appui (`escapeSeen`, posé là où le drapeau `$80` du port de contrôle l'est déjà, remis à zéro par le reset `0xFF` de P1) et `BOOTSelect` le consulte **avant** ses deux attentes, en plus de la file. Échap compte donc dès que le clavier est énuméré, logos compris. L'Échap consommé par le menu est aussi **retiré du port de contrôle**, pour que le 6502 ne le reçoive pas une seconde fois. Non testable automatiquement : `BOOTSelect` s'exécute dans `DSPReset`, **avant le premier cycle 6502**, donc hors de portée des tests `tests/api` comme du `--type-keys` de Phosphoneo (vérifié : une frappe programmée en cycles n'atteint jamais cette fenêtre). | **DONE** 0.14.1, à valider carte |
| T-69 | P2 | **NeoDOS 0.24.0 embarqué : `MODE` rend le mode Hercules atteignable depuis le DOS** (constat carte bmarty 2026-09-24 : « je n'ai pas vmode sous dos »). Vérification faite, aucune des 35 commandes de NeoDOS ne touchait au mode vidéo : le **mode 1 de Trinity n'était joignable que par NeoBASIC** (`VMODE`) — donc invisible pour qui reste sous DOS, ce qui est le cas au démarrage. `MODE` (sans argument) affiche le mode courant et ses dimensions par `5,10 Get Mode`, `MODE n` en change par `5,9 Set Mode` ; un numéro refusé par le firmware donne `Invalid video mode` et `ERRORLEVEL 1`. Côté Trinity, seule l'image résidente change (`neodos_binary.h` régénérée depuis `~/Neo6502Msdos/build/neodos.bin`). Au passage, le test `09_exit` de NeoDOS échouait depuis la 0.23.0 : sa référence attendait encore `0.22.0`, parce qu'`EXIT` recharge l'image **embarquée dans le firmware** et non le binaire local — d'où l'obligation de régénérer `neodos_binary.h` à chaque version de NeoDOS. NeoDOS 49/49. | **DONE** 0.15.0, à valider carte |
| T-70 | P3 | **Pilote UFI/CBI : les lecteurs de disquette USB sur la carte** (décision bmarty 2026-09-24 : **laissé en backlog**, ne pas ouvrir maintenant). Mesure faite et **sans ambiguïté** — le lecteur d'essai, un **TEAC `0644:0000`**, se déclare `bInterfaceClass 8` / `bInterfaceSubClass 4` (**Floppy, UFI**) / `bInterfaceProtocol 0` (**Control/Bulk/Interrupt**), avec trois points de terminaison : bulk out `0x01`, bulk in `0x82`, **interruption in `0x83`** (2 octets). Or `msch_open` de TinyUSB 0.21 exige `MSC_SUBCLASS_SCSI` (6) **et** `MSC_PROTOCOL_BOT` (0x50) (`msc_host.c:384`) : le lecteur est rejeté à l'ouverture, et **ne l'a jamais été autrement** — ce n'est pas une régression. L'espoir d'un correctif court (UFI **+ BOT**, où seul le test de sous-classe aurait bloqué, quelques lignes) est **écarté par la mesure** : CBI est un transport entièrement différent de BOT — commande par **transfert de contrôle** (requête class-specific ADSC), données en bulk, **statut par l'endpoint d'interruption** — donc rien de la machine à états MSC de TinyUSB ne se réutilise. Il faut un pilote de classe applicatif complet (`usbh_app_driver_get_cb`, déjà utilisé par Trinity pour le modem), estimé **450 à 600 lignes**. À rouvrir seulement sur demande. | **BACKLOG** (mesuré 2026-09-24, non planifié) |
| T-72 | — | **Fausse alerte : NeoLegacy n'affichait plus ses images parce que son fichier `.dat` était incomplet** (bmarty 2026-09-24 : le réseau a lâché pendant le téléchargement). **Aucune régression du firmware.** J'avais ouvert cette entrée en accusant le placement en flash (T-48 rebasculé par les 1 344 octets de la police MDA) : l'hypothèse était cohérente — le jeu tournait en 0.13.0, et T-48 dit qu'un décalage de quelques dizaines d'octets suffit — mais **elle était fausse**, et la bissection en trois flashs qu'elle allait déclencher aurait été du temps perdu. Leçon : avant de soupçonner un défaut connu qui « explique tout », vérifier l'état des **données** que le programme lit. Un fichier tronqué et une panne de placement produisent le même symptôme à l'écran. | **CLOS, sans objet** |
| T-71 | P1 | **`DIR` casse l'affichage en mode 1 (Hercules)** (retour carte bmarty 2026-09-24, sur 0.15.0 avec la commande `MODE` qui rend enfin le mode 1 accessible depuis le DOS — donc défaut **probablement ancien et jamais vu**). Taper `MODE 0` rétablit un affichage correct, et la machine répond pendant la panne : **le 6502 et le clavier vont bien, c'est l'affichage seul**. Non reproductible sous Phosphoneo : la capture texte **et** le rendu 720×350 de `MODE 1` + `DIR` sont corrects au pixel près — le défaut est donc dans le chemin **DVI de la carte**, que l'émulateur ne simule pas. Deux pistes, non départagées : (a) le mode 1 n'a que **deux** tampons de ligne (`monoLine1`/`monoLine2`, alternés par la parité de `lineCounter`), là où le mode 0 en a **quatre** depuis T-53 — si l'encodeur décroche, le callback réécrit le tampon qu'il est en train de lire ; le commentaire du code note déjà que le mode 1 est à la limite du débit (« encoding a 720 pixel line three times was too slow », lignes rouges carte du 2026-09-19) ; (b) le défilement de la console repeint **1 920 cellules de 9 × 14** caractère par caractère (`CONDeleteLine` → `CONCopy` → `CONPaintCharacter`), en **lisant la police en flash** sur core 0 — soit la contention XIP que T-44 a déjà corrigée pour le curseur, et un `DIR` défile une vingtaine de fois. Écarté : aucun débordement des tampons mono (196 octets pour un stride de 90). | **MESURE** 0.16.10, à relever carte | **Séquence reproductible (bmarty 2026-09-24, 0.15.0)** : reboot → `MODE 1` **OK** → `DIR` → **écran noir** → `MODE 0` **OK** → `MODE 1` **OK** → `DIR` **OK**. Le **second** `DIR` passe, dans le même mode et le même répertoire : ce n'est donc ni le mode 1 en soi, ni le défilement, ni la police — **c'est la charge disque**. Le premier `DIR` après le démarrage lit la FAT et le répertoire **à froid** ; le second les trouve dans le cache de FatFs et ne fait presque plus d'E/S. La rafale de secteurs affame core 1, et le mode 1 est le plus exposé : 720×480p60, encodage d'une ligne de 720 pixels au rythme natif, **deux** tampons de ligne seulement. À vérifier d'un seul test, gratuit : après la séquence, faire en mode 1 un `DIR` sur un répertoire **jamais listé** (cache froid) — s'il noircit à nouveau, la charge disque est tenue. Noter que le symptôme est un écran **entièrement** noir, pas quelques lignes fausses : plus qu'un décrochage ponctuel, ce qui oriente vers une perte de synchronisation, voire le `queue_add_blocking_u32` du callback qui bloquerait en interruption sur core 1. **MESURÉ PAR SWD LE 2026-09-25, et l'hypothèse initiale est renversée.** Relevé pendant le `DIR` qui noircit l'écran : `trames` **+60/61 par seconde sans une seule interruption**, `vram` qui passe de **1/16 à 11/16 points non nuls** — la mémoire vidéo contient bel et bien le catalogue — et `late` qui monte de **52 à 66** pour 22 secteurs lus. Donc **ni firmware figé, ni encodeur arrêté, ni mémoire vidéo effacée** : le contenu est affiché et le signal est produit, mais **le moniteur décroche**. Les ~14 lignes livrées en retard perturbent la synchronisation ; en mode 1, seul timing **natif** (720×480p60, 270 MHz, sans marge), le moniteur perd le verrouillage et **ne le retrouve pas seul** — ce qui explique enfin pourquoi seul un changement de mode ramène l'image, et pourquoi le second `DIR` passe (FAT en cache, presque plus d'E/S, donc plus de retards). Correction retenue, la piste (a) : **quatre tampons de ligne au lieu de deux** (`monoLine[4]`, index `lineCounter & 3`), comme le mode 0 depuis T-53 — avec deux tampons le callback revient sur celui que l'encodeur est en train de lire dès qu'il décroche, quatre lui donnent trois lignes d'avance. Coût 392 o. **Critère de validation mesurable** : `late` ne doit plus monter pendant un `DIR` à froid en mode 1. **Erreur de méthode à retenir** : j'ai d'abord mesuré « mémoire vidéo entièrement à zéro » et conclu à un effacement — c'était faux, la mesure ayant été prise après que mes propres points d'arrêt aient figé les deux cœurs et cassé l'affichage. **On ne diagnostique pas une panne d'affichage avec un outil qui arrête l'encodeur** : seules les lectures mémoire non intrusives valent ici. **Essai 0.16.6 sur carte : les quatre tampons ne suffisent pas.** `late` monte encore de **59 à 70** pendant le `DIR` à froid (contre +14 avant, donc trois épisodes de moins seulement) et **l'écran noircit toujours**. La cure doit donc réduire le nombre de lignes en retard, pas leur ménager plus de place. **0.16.7 : priorité du bus mémoire à core 1** — `bus_ctrl_hw->priority = BUSCTRL_BUS_PRIORITY_PROC1_BITS`. C'est la conclusion de T-56 qui n'avait **jamais été essayée** : donner la priorité au DMA avait empiré les traits, ce qui désignait core 1 comme l'affamé ; il fallait donc la lui donner à lui. Core 1 doit livrer une ligne toutes les 31 µs pendant que core 0 martèle la SRAM pour FatFs et TinyUSB, et l'arbitre du RP2040 les sert à tour de rôle sauf instruction contraire. Un registre, coût mémoire nul. **À surveiller** : ralentir core 0 ne doit pas affamer en retour la boucle du bus 6502 — `txstall`/`rxstall` doivent rester à zéro. **0.16.7 ANNULÉE : donner la priorité du bus à core 1 est pire que le mal.** Sur carte, **l'écran noircit dès `MODE 1`, sans le moindre accès disque**, et même les lectures SWD échouent (`error reading data`). La priorité s'applique à tout ce qui partage le bus : en mode 1 core 1 tourne à plein et monopolise la SRAM, si bien que core 0 ne peut plus écrire la mémoire vidéo, ni le port de debug lire la sienne. Comme T-56, l'essai instruit : les deux cœurs se disputent bien la même bande passante, mais **aucun ne peut être privilégié** — il faut diminuer la demande, pas arbitrer entre elles. **Piste ouverte par une observation de bmarty (2026-09-25)** : « en mode 0 mon moniteur affiche 60 ; en mode 1 l'indicateur n'apparaît plus ». Le moniteur ne **reconnaît pas** le signal du mode 1 — ce qui suggère plus qu'un décrochage passager : le **timing lui-même** serait marginal. Le mode 1 est le seul mode natif (720×480p60, RP2040 poussé à 270 MHz), sans marge ; les lignes en retard n'achèveraient qu'un signal déjà limite. À creuser : marges de synchronisation et fréquence pixel, plutôt que bande passante. **0.16.6 ANNULÉE À SON TOUR : les quatre tampons aggravaient.** Mesure SWD du 2026-09-25 pendant un `DIR` à froid en mode 1, sur un **second écran** qui reconnaît pourtant le signal (indicateur 60 Hz présent, contrairement au premier) : **le compteur de trames se fige net** — `trames=661 (+0)` pendant onze secondes, core 1 ne l'incrémente plus — puis **la carte redémarre d'elle-même** (compteurs remis à zéro, retour en mode 0). Avec deux tampons, le même `DIR` laissait le firmware **vivant** (60 trames/s, mémoire vidéo pleine) et ne perdait que l'image. Transformer un écran noir en blocage complet n'est pas un progrès : retour à deux tampons en 0.16.9. Piste à instruire : le callback publie par `queue_add_blocking_u32` et **bloque en interruption sur core 1** si la file est pleine ; quatre tampons changent le rythme auquel ils lui sont rendus et peuvent créer l'interblocage que deux évitaient. **La conclusion « c'est le moniteur qui décroche » est donc fausse** : sur un écran qui accepte le signal, le firmware se bloque quand même. **Leçon de méthode** : quatre explications successives ont été démenties par la mesure suivante (famine mémoire, mémoire vidéo effacée, décrochage du moniteur, contenu corrompu). Ne plus expliquer avant d'avoir **comparé deux firmwares dans des conditions identiques** — ce qui n'a jamais été fait proprement sur ce défaut. **0.16.10 : on arrête d'expliquer, on instrumente — lots 1+2 dans une seule version.** **Lot 1, la publication ne bloque plus.** `_scanline_callback` publiait par `queue_add_blocking_u32` : il tourne **en interruption sur core 1**, et `q_colour_valid` (**8 places**, `picodvi/software/libdvi/dvi.c:36`) n'a qu'un seul consommateur, `_encode_loop`, **sur ce même core 1**, que l'interruption préempte. Dès huit lignes de retard, le callback attend une place que seul le code qu'il a préempté pourrait libérer : **interblocage structurel**, qu'aucune bande passante ne corrige — et c'est la forme exacte du gel de 0.16.6 (trames figées onze secondes, puis redémarrage). Désormais `queue_try_add_u32` : la ligne est **jetée et comptée** (`rejets`). Le gel devient impossible, et le compteur tranche dans les deux sens — `rejets > 0` tient le mécanisme, `rejets == 0` pendant un `DIR` noir l'élimine définitivement. **Lot 2, deux ou quatre tampons dans le même binaire.** 0.16.6 ajoutait 392 o de données et **déplaçait donc toute la carte mémoire**, alors que **T-48 a prouvé sur cette carte que le placement seul suffit à faire apparaître ou disparaître la panne** : l'A/B 0.16.6 / 0.16.9 n'était pas un essai à une seule variable, et son verdict (« les quatre aggravaient ») est **à rejuger**. Les quatre tampons sont maintenant **toujours alloués** (`monoLine[4]`, 784 o) et le nombre réellement utilisé est choisi **à l'exécution** (`monoLineMask`, bascule au début de trame) par `!2` / `!4` sur le port de debug : même binaire, même taille, même placement, **une seule variable**. Coût : 588 o de RAM de plus, et **il ne reste que 316 o sous `RAM_LIMIT`** — c'est un binaire d'expérimentation, à ramener à un seul choix une fois le verdict rendu. **Instrumentation** : `!v` sort `rejets` et `tampons` ; la télémétrie périodique gagne une colonne `R=`, donc les rejets se lisent **pendant** le `DIR`, sans SWD et écran noir (le clavier UART de T-74 permet de taper `DIR` depuis le PC). **Correction de fait (bmarty, 2026-09-25) : il n'y a qu'UN SEUL écran d'affichage.** Les mentions d'un « second écran » ci-dessus (0.16.6, 0.16.9) sont donc fausses — ce qu'elles décrivaient reste à établir, et n'est pas réécrit ici faute de le savoir. Conséquences : l'A/B 0.16.6 / 0.16.9 **était** bien à écran constant (la différence de symptôme vient donc du firmware, sous réserve du placement ci-dessus), mais l'hypothèse « le moniteur décroche » **ne peut plus être testée en changeant d'écran**. **Protocole carte (deux passages, pas de reflashage entre les deux)** : bras A — reboot, `!z`, `!2`, `MODE 1`, `!v`, `DIR` sur un répertoire **jamais listé**, `!v` ; bras B — reboot, `!z`, `!4`, `MODE 1`, `!v`, `DIR` sur un **autre** répertoire jamais listé, `!v`. Relever à chaque fois `trames`, `late`, `rejets`, `secteurs`, et si l'écran noircit. **Critères** : (1) la carte ne doit plus **jamais** se figer ni redémarrer, quel que soit le masque — sinon le gel de 0.16.6 n'était pas l'interblocage de la file et il faut chercher ailleurs ; (2) `rejets > 0` pendant le noir tient le mécanisme ; (3) `rejets == 0` + `trames` qui avance + écran noir **tue** la piste file/interblocage et envoie au lot 3. **Lot 3, déjà prévu, à n'écrire qu'après le relevé** : séparer les trois charges que `DIR` mélange — E/S sans affichage, défilement sans E/S, puis le test décisif, **ralentir volontairement l'E/S** en mode 1 : si le noir disparaît, la charge disque est cause *nécessaire* ; sinon elle est innocentée. **Écarté volontairement de 0.16.10 pour ne pas ajouter de variable** : en mode 1, `_encode_loop` rend chaque `monoLine` à `q_colour_free` alors que ce n'est pas un pool à recycler (le callback vide la file sans en lire le contenu) — un aller-retour de file par ligne, et un blocage possible de l'encodeur, tous deux supprimables. 
| T-68b | P0 | **Échap au démarrage : toujours pas, malgré T-68** (retour carte bmarty 2026-09-24). Assez d'hypothèses : le firmware le dira lui-même. La ligne `USB settled` affiche désormais deux témoins, relevés **avant** que `BOOTSelect` ne décide : **`KEY`** = le clavier a produit au moins un caractère pendant la phase silencieuse (donc il était énuméré et la frappe est arrivée) ; **`ESC`** = le drapeau que lit le menu est posé. Trois issues, trois diagnostics : (1) **`ESC` affiché et pas de menu** → la capture est bonne, la faute est dans `BOOTSelect` ; (2) **`KEY` sans `ESC`** → le clavier marchait, mais l'appui n'a pas produit `KEY_ESC` — mappage ou touche ; (3) **ni l'un ni l'autre** → le clavier n'était pas encore énuméré au moment de l'appui, et aucun drapeau ne peut rattraper une frappe que le matériel n'a pas vue : il faudra alors afficher l'invite **pendant** les logos plutôt que d'attendre P2. Coût : deux tests et deux chaînes, rien en RAM. | **MESURE** 0.15.1, à relever carte |
| T-74 | P1 | **Terminal sur le port de debug** (demande bmarty 2026-09-25 : « un terminal permettant d'exécuter des commandes pour exploiter le Neo6502 », puis « vérifier l'état des famines ou l'état de la RAM »). Les caractères reçus sur l'UART entrent dans la **file clavier** : le Neo se pilote depuis le PC comme depuis son propre clavier, **écran noir compris** — c'est tout l'intérêt, puisque c'est l'état où T-71 laisse la machine. La sortie réutilise ce qui existait déjà : l'écho console (`consoleEcho`, API `2,20`, `CONSetDebugEcho`) et `FDBWrite`, dont seule la destination change — le port de debug au lieu du port série du 6502. Une ligne de télémétrie par seconde : `L=` lignes DVI en retard, `S=` secteurs, `M=` mode. Commandes préfixées par `!`, une lettre, sans tampon de ligne : **`!s`** famines (`late`, `txstall`, `rxstall`, `secteurs`), **`!m`** mémoire (`bss_end`, `brk`, `pile`, `libre` — le tas, d'où sortent les tampons TMDS), **`!z`** remise à zéro des compteurs de bus, **`!!`** un vrai `!` pour le 6502. Anneau porté à 2 Ko (un `DIR` produit ~1,5 Ko d'un coup alors que le port ne sort que ~3 Ko/s), d'où `RAM_LIMIT` 231 600 → 233 500, marge vérifiée : 29,2 Ko de tas. **Réserve** : UART0 est partagé avec le port série du 6502 (groupe 10) — un programme qui l'utilise reconfigurera le débit et brouillera la trace. | **DONE** 0.16.0, à valider carte | **Commandes ajoutées le 2026-09-25**, chacune pour un ticket ouvert : **`!v`** vidéo — mode, dimensions, `late`, et surtout le **compteur de trames**, qui tranche T-71 mieux que tout le reste (core 1 l'incrémente au début de chaque trame : s'il monte pendant l'écran noir, le signal vit et l'image est seulement fausse ; s'il gèle, l'encodeur s'est arrêté) ; **`!k`** clavier — présent, Échap vu, file non vide, soit la question que T-68 ne pouvait pas poser depuis l'écran ; **`!p`** placement — adresses de `DSPSync`, `DBGFlush`, `DBGInitialise` (`2xxxxxxx` = RAM, `10xxxxxx` = flash), qui vérifie que **le port de debug n'a pas lui-même enfreint la règle T-46 qu'il sert à surveiller**, et qui donne un point de comparaison pour la sensibilité au placement de T-48 ; **`!a`** enchaîne les cinq rapports. Coût ~340 o de code en RAM, d'où `RAM_LIMIT` 233 500 → 234 500 ; 28,9 Ko de tas restants. Outil PC versionné : `firmware/scripts/neoterm.py` (`capture` horodatée, `send`, `watch`) — la capture en fichier est la forme utile quand la carte plante, puisqu'elle survit à l'écran. **`!u` et `!f` ajoutés le 2026-09-25** (demande bmarty) : **`!u`** usb — barrière franchie, événements d'énumération vus, clavier monté (T-70, T-31) ; **`!f`** stockage — pour chaque lecteur FatFs l'adresse USB qui le sert (0 = libre), l'état occupé, et les secteurs déplacés. **Un lecteur resté occupé longtemps après un transfert est la signature même de T-31** (clavier muet pendant les accès disque). Contrainte de conception : ces rapports partent de `DSPSync`, donc ils ne lisent **que ce que le firmware garde déjà en RAM** — interroger TinyUSB ou FatFs directement aurait été plus riche, mais tout TinyUSB vit en flash (`hcd_rp2040_irq` à `0x1001b38c`, mesure T-48) et appeler du code flash depuis `DSPSync` est exactement la faute qui tuait tous les programmes en 0.10.x. D'où les accesseurs `USBEventCount()` (usbsettle, motif `TIMECRITICAL` de dispatch.cpp) et `STODebugDrive()`/`STODebugBusy()` (usb_storage, `__not_in_flash_func`). Coût 288 o ; 940 o de marge sous `RAM_LIMIT`, 28,6 Ko de tas. **Défaut corrigé le 2026-09-25, avant même la première trace** : le port restait muet parce que son **débit n'était pas recalculé après le changement d'horloge**. `DVIStart` appelle `set_sys_clock_khz` (252 ou 270 MHz selon le mode), `clk_peri` suit, et le diviseur UART calculé en P0 cesse d'être juste. `serial.cpp` connaît le problème et le règle par `SERClockChanged`, mais **seulement pour un port que le 6502 a ouvert** (`currentBaudRate != 0`) — ce qui n'est jamais le cas du port de debug, qui s'initialise seul. D'où `DBGClockChanged()`, accroché à `HWClockChanged()` à côté de `SERClockChanged()` et `SNDClockChanged()`. Trouvé en lisant le code après un silence complet sur la liaison ; reste à confirmer que c'était bien la seule cause, le câblage UEXT n'ayant pas encore été validé. **Seconde cause du silence, la vraie (2026-09-25)** : `GPIOMapping[]` de `pininterface.cpp` associe les broches UEXT du 6502 aux GPIO du RP2040, et ses entrées 3 et 4 sont **28 et 29** — les broches de l'UART. `IOInitialise()` s'exécute en **P3**, donc **après** `DBGInitialise()` en P0, et remet chaque broche de cette table en entrée : or `gpio_init()` réinitialise la fonction du GPIO en SIO, **détruisant la fonction UART**. Le port était donc éteint à chaque démarrage, et aucun câblage n'y aurait rien changé. La table prévoyait pourtant le cas (`IOPINDisabled`, commentaire « probably in use by SPI, Serial, I2C ») : `DBGOwnsGPIO()` marque les deux broches réservées et `IOInitialise` les saute. **Méthode** : c'est le **test de bouclage** de l'adaptateur (`NEOTEST-12345` renvoyé intact) qui a permis de trouver — en prouvant que l'adaptateur, le pilote, le port et le débit étaient bons, il a forcé la recherche du côté du firmware au lieu du câblage, du brochage et du schéma de la carte. Conséquence à connaître : les broches 3 et 4 de l'UEXT ne sont plus offertes au 6502 tant que le port de debug est actif.
| T-73 | P1 | **Voie de debug sur UART0** (demande bmarty 2026-09-24, après une série de défauts que seul l'écran pouvait rapporter — or c'est l'écran qui tombe). **Pas par l'USB-C**, contrairement à l'idée de départ : le RP2040 n'a **qu'un seul contrôleur USB**, déjà `CFG_TUSB_RHPORT0_MODE = OPT_MODE_HOST` pour le clavier, le hub et la clé, et le connecteur de programmation **est** ce contrôleur — hôte et CDC device s'excluent, et basculer coûterait précisément les périphériques dont on veut observer l'activité. UART0 (GPIO 28/29, **UEXT broches 3 et 4**, 3,3 V) est libre. Matériel validé : CP2104 (Cnewtec CNT-003) sur `/dev/ttyUSB0`, pilote `cp210x`, 115 200 bauds — inutile de monter plus haut, le vidage à 95 Hz plafonne à ~3 Ko/s. **La règle T-46 vaut ici plus qu'ailleurs** : `DSPSync` est en RAM, donc `DBGWrite`, `DBGFlush`, `DBGPoll` et la télémétrie sont `__not_in_flash_func` et **n'utilisent pas `snprintf`** (qui vit en flash) — conversion hexadécimale maison. `DBGWrite` n'écrit que dans un anneau RAM, sans accès registre ni attente ; `DBGFlush` ne pousse que ce que le FIFO matériel accepte. Une trace qui calerait le bus fausserait la mesure qu'elle sert à prendre. | **DONE** 0.16.0, à valider carte |
| T-75 | P1 | **Balayage des broches UEXT : le firmware dit lui-même où il parle** (2026-09-25). Le port de debug est resté muet à travers **deux vrais défauts de firmware** (T-74 : débit non recalculé après le changement d'horloge, puis broches reprises par `IOInitialise` en P3) **et toutes les permutations de câblage**, sans qu'on puisse départager « mauvaise broche » de « troisième bug » — et sans multimètre pour interroger la carte. Renversement de la question : pendant les **30 premières secondes**, le firmware déplace la fonction UART TX d'une broche UEXT à la suivante, une par seconde, en annonçant `UEXT GPIO=nn`. Le fil reste où il est ; l'annonce qui arrive dans le terminal **nomme la broche**. Candidates : 28, 29 (UART), puis 22-27 (I2C et SPI du firmware — les piloter une seconde est sans danger, rien n'étant branché sur le port). L'écriture va directement au matériel, pas par l'anneau, avec `uart_tx_wait_blocking` : le message doit avoir quitté le registre à décalage **avant** que la broche ne change sous lui. Cette attente bloquante est la raison pour laquelle le balayage ne tourne **que** pendant ces 30 s, jamais en fonctionnement normal. Quatre passes, puis retour au port sur 28/29. | **MESURE** 0.16.5, à relever carte |
| T-76 | P1 | **Lecture en direct par SWD : `firmware/scripts/neoswd.sh`** (2026-09-25). Sonde = un Pico flashé avec le `debugprobe` de Raspberry Pi (CMSIS-DAP), câblé `GP2→SWC`, `GP3→SWD`, `GND→GND` ; OpenOCD lit la mémoire du RP2040 **pendant que la carte tourne**, sans rien demander au firmware. Affiche chaque seconde le compteur de trames et son delta, `lateTotal`, les secteurs déplacés, la largeur du mode et le remplissage de la mémoire vidéo (16 points répartis sur la page). Les adresses sont **relues dans l'ELF** à chaque lancement, puisqu'elles changent à chaque compilation. C'est cet outil qui a tranché T-71 en une mesure, après deux jours d'hypothèses. **Deux pièges appris à la dure** : (1) un point d'arrêt matériel arrête **les deux cœurs**, donc l'encodeur DVI — inutilisable pour diagnostiquer l'affichage, et c'est ce qui m'a fait conclure à tort à une mémoire vidéo effacée ; (2) tuer OpenOCD brutalement laisse la cible **haltée**, clavier mort et carte figée, jusqu'au redémarrage. Toujours relâcher avant de se déconnecter. Le Cortex-M0+ ne pose d'ailleurs des points d'arrêt matériels **qu'en flash** (sous `0x1FFFFFFE`), jamais en RAM. | **DONE** 0.16.6 |
| T-01 | P1 | Modem USB CDC (groupe 14) + routage UART 10,19 (F-90/F-93 du fork). | **DONE** 0.0.1, validé carte (modem reconnu, netinfo, IP) |
| T-02 | P1 | TinyUSB 0.21 : clé USB derrière le hub (1 ms/secteur au lieu de 2,5 s). | **DONE** 0.1.0, validé carte |
| T-03 | P1 | BASIC découplé : `neobasic.bin` sur le stockage prioritaire (projet Neo6502Basic). | **DONE** 0.2.0, validé carte ; **remplacé en 0.4.0 par T-15** (NeoBASIC = `boot/neobasic.bin`, plus de copie racine) |
| T-04 | P1 | Menu de démarrage `boot/` (.neo/.bin) et `boot/auto.txt` (Échap = menu). | **DONE** 0.2.0, validé carte |
| T-05 | P1 | Lecture par blocs CDC : servir l'hôte USB pendant l'attente (R16). | **DONE** 0.2.1, validé carte (ProphetGui) |
| T-06 | P1 | ProphetGui de bout en bout sur carte (F-94) : catalogue, fiche, téléchargement. | **catalogue OK** 2026-09-19 (Trinity 0.2.1, modem Pico W, 3617.fr) ; fiche et téléchargement à confirmer |
| T-07 | P2 | Mode vidéo 1 Hercules 720×350 (F-51/52/53/55 du fork, sans le mode 2). | **DONE** 0.3.0, validé carte (10 builds de bissection : IRQ, encodeur, core 1, DMA) |
| T-08 | P2 | Primitives BASIC `at`/`atline$`/`atwait` (Neo6502Basic) : livrées par `neobasic.bin`, à consigner ici par version. | WIP (Neo6502Basic) |
| T-09 | P3 | Reprises du fork, une par une et validées carte : 3,27 File Read Paged, volumes 3,24-3,26, locale FR/Latin-1, tick/IRQ trame, banques, toolbox. | TODO — sur demande |
| T-10 | P2 | **NeoDOS** (`docs/MEMO-NEODOS-2026-09-19.md`) : ~~valider sur carte `boot/neodos.neo` + `auto.txt`~~ (remplacé par T-15 : NeoDOS est embarqué dans le firmware) ; **volumes, date/heure, horodatage FAT livrés en 0.6.0 (T-18)** ; reprises demandées : volumes 3,24-26 (T-09), date/heure 1,20-21 + horodatage FAT, dates dans 3,18/3,16 (ou `3,28 Stat Extended`), `..` et casse dans l'émulateur `neo`, contrat `$FF08` documenté. | TODO |
| T-11 | P2 | **`1,3` et `boot/auto.txt` à chaque appel** (demande NeoDOS 0.8.2, `docs/MEMO-NEODOS-2026-09-19.md`) : aujourd'hui `BOOTLoadChoice()` n'agit qu'au premier `1,3` ; un programme qui se termine par `1,3` + `jmp (0)` revient donc à NeoBASIC, jamais au programme de démarrage. Proposition : `1,3` recharge le choix de `boot/auto.txt` à chaque appel (NeoDOS revient comme NeoBASIC revient, sans stub en `$0100`) ; `auto.txt` absent ou Échap = NeoBASIC. À trancher : sémantique de `1,3` pour les programmes qui comptent revenir à BASIC (option : nouvelle fonction `1,22 Reload Boot Program`, `1,3` inchangé). Test : `neo` + `boot/`. **0.4.0 (T-15)** : `1,3` = NeoDOS embarqué (premier appel : choix `boot/`) ; le stub `$0100` de NeoDOS et `boot/neodos.neo` deviennent inutiles ; reste ouvert pour les programmes qui comptaient revenir à NeoBASIC (→ `boot/neobasic.bin` via le menu, ou `1,22`). | TODO (réduit) |

| T-12 | P2 | **Toolbox dans le firmware** (décision bmarty 2026-09-20 : la toolbox est une fonctionnalité firmware, pas un programme 6502). Reprise des gestionnaires du fork (`archive/bmarty-main-2026-09-19`, `firmware/common/sources/interface/toolbox_*.cpp`) **un par un, chacun validé carte et golden** : Windows → Events → QuickDraw → Menus → Controls → Dialogs → Resources. Coût RAM chiffré sur le fork : ≈ 1,6 Ko d'état (`windows[8]` 360 o, `controls[24]` 480 o, `dialogs[2]` 160 o, `queue[32]` + timers 500 o, `menus[6]` 50 o), code en flash (XIP, non mesuré, 1,8 Mo libres). Règles à conserver : descripteurs (menus, dialogues, textes, ressources) dans la RAM du 6502 ou sur le stockage (Resource Manager), jamais copiés côté firmware ; pas de sauvegarde d'écran sous les menus/dialogues (redessin par l'application sur `update`) — si un jour nécessaire, prise dans `gfxObjectMemory`, jamais en `static`. À traiter : QuickDraw écrit en 8 bpp (mode 0), à adapter au mode 1 Hercules 1 bpp (jamais vu par le fork) ; `std::string` dans `toolbox_resources.cpp` à remplacer par un tampon fixe (voir T-13) ; Dialog Manager dépendait de la locale FR/Latin-1 (F-17) : reprendre sans, ou avec la locale (T-09, à trancher). Tests : cas golden `menu`/`ctl`/`dlg`/`res` de Phosphoneo dans `archive/wt-toolbox-2026-09-20`, à re-baseliner sur Trinity. Prérequis : T-13. | **WIP** : 0.5.0 = QuickDraw (32) + Window Manager (34), 0.5.1 = Event Manager (33), 0.5.2 = Menu Manager (35), 0.5.3 = Control Manager (36), 0.5.4 = Dialog Manager (37), 0.5.5 = Resource Manager (38) : **les 7 gestionnaires sont repris**, mode 0, `make test-toolbox` 8/8 identiques au fork, **à valider carte** (`trinity-0.5.5-resources-USB.uf2`, `cle-usb/{quickdraw,quickdraw2,wm,evtimer,events,menu,ctl,dlg,res}.neo6502` + `test.res`) ; 0.5.6 = mode 1 Hercules (gris → damier via `_QDPut`, `CopyBits` pixel par pixel, test `hercules.asm`). **T-12 complète dans `neo` (9/9)** ; carte : à valider (`trinity-0.5.6-hercules-USB.uf2`, `cle-usb/*.neo6502` + `test.res`) |
| T-13 | P2 | **Budget mémoire** (analyse du 2026-09-20 sur Trinity 0.3.0, `STORAGE=USB`) : `.bss` 212,5 Ko + `.data` 17,9 Ko sur 256 Ko de SRAM principale → **≈ 41 Ko libres** (tas + piles + tout ajout) ; flash 207 Ko sur 2 Mo (10 %, pas une contrainte). Morpheus amont `dc70908` : UF2 370 176 o, text 184 844, bss 207 596 ; Trinity 0.3.0 : UF2 413 696 o, text 206 636, bss 214 728 (+7 Ko RAM, +22 Ko flash). Pools structurels 171 Ko (`graphicsMemory` 76 800, `cpuMemory` 65 536, `gfxObjectMemory` 32 768) : toute nouvelle fonctionnalité y prend sa mémoire, jamais un nouveau tableau `static`. Livrables : (1) cible `make size` (`arm-none-eabi-size firmware.elf`) et ligne « budget » dans `README-TRINITY.md` à chaque version ; (2) seuil bloquant dans le Makefile (échec si `.bss` + `.data` > 230 Ko, valeur à confirmer) ; (3) `blackChannel[360]` (`dvi_320x240x256.cpp:87`) en `const` flash : −1 440 o (rempli une fois de `TMDS_BLACK_WORD`, seulement source de `memcpy`) ; (4) `fileinterface.cpp` sans `std::string` (tampons `char` fixes dans les 8 fonctions `FIO*`) : −3 792 o de dérouleur d'exceptions C++ en `.data` (tiré par `std::__throw_bad_alloc` → libstdc++ → libgcc, que le script de lien du SDK place en RAM ; `-fno-exceptions -fno-rtti` seuls = 0 octet gagné, vérifié par compilation) + plus d'allocations tas ; (5) `-fno-use-cxa-atexit` : −400 o (`__atexit0`). Gain total ≈ 5,6 Ko (+14 % de marge). Écarté : réécriture en assembleur ARM (la RAM est occupée par des données, pas du code ; gain ≤ 2 Ko, perte des tests Phosphoneo/`neo` sur x86), réduction de `FIO_NUM_FILES`/`MAX_SPRITES`/console (contrats API). Validation carte obligatoire (débordement de pile et `malloc` NULL invisibles en émulateur). | **DONE 0.3.1**, à valider carte : cxxthrow.cpp + cxx_no_extern_string.h (−4 092 o `.data`, plus de pool d'exceptions ni de dérouleur), `blackChannel` remplacé par un remplissage constant (−1 440 o, pas de lecture flash en callback de ligne), `make size` + `RAM_LIMIT` 230 000 ; total −5 580 o → 37 124 o libres (le « 41 Ko » initial omettait `.data` : 31 544 réels en 0.3.0). Non fait : `FIO*` sans `std::string` (83 occurrences dans 10 fichiers amont + émulateur, divergence trop forte ; inutile après le remède de lien), `-fno-use-cxa-atexit` (déjà appliqué par le SDK ; `__atexit0` 400 o est celui de newlib) |
| T-14 | P3 | **Multitâche 6502** (demande bmarty 2026-09-20, reprise de l'épopée F6 du fork, `archive/bmarty-main-2026-09-19` : `docs-bmarty/F-60-tick-irq.md`, `F-61-rtos.md`, `F-10`). Deux volets. **(a) Firmware RP2040** : tick IRQ périodique sur IRQB (GPIO25) — `1,12 Set Tick` / `1,13 Get Tick` (F-60, `irq.cpp`, `group1_system.inc`), IRQ de trame `1,16` / `1,17` (F-10, depuis `_scanline_callback` de core 1, ~10 cycles ARM, hors boucle bus) ; IRQB relâchée à la lecture de `$FFFF` (pas d'acquittement explicite) ; **jamais testé sur carte** (`wdc65C02cpu_set_irq()` n'est appelé nulle part dans l'amont ; timer matériel écrit sans essai ; marge de 11 nop dans la boucle bus `processor_pio.cpp` après F-60, à re-mesurer). Coût RAM RP2040 : quelques octets. **(b) Noyau 6502** (`kernel/rtos.asm` ≈ 600 o + `rtos_data.asm` `$FF10-$FF6D`, compilés dans `kernel_binary.h`, copiés en `$FC00-$FFFF`) : 4 tâches, tourniquet sur le tick, `KTaskInit/Create/Yield/Sleep/Exit/Lock/Unlock/Ticks`, `KSemWait/Signal` (10 vecteurs `$FFC1-$FFDC`), pile `$0100` en 4 × 64 o, page zéro privée `$E0-$EF`, `WAI` si rien de prêt ; **l'API `$FF00` n'est pas réentrante** → `KTaskLock` autour des appels ; BASIC incompatible (page zéro, pile) ; 7 octets de marge avant `$FF00` dans le fork — à vérifier sur le noyau amont de Trinity. Coût RAM RP2040 : nul (vit dans `cpuMemory`). Côté RP2040 rien de préemptif possible (core 0 sert le bus cycle par cycle, core 1 rend le DVI) : coopératif seulement. Ordre : (a) validé carte (oscilloscope ou compteur d'IRQ sur IRQB, cohabitation avec le mode 1 Hercules et le modem CDC), puis (b) ; tests : `irqtick.neo6502`, `frameirq.asm`, démos `rtos`/`rtos_idle` (neo ↔ Phosphoneo identiques dans le fork) ; `neo` doit reprendre `WAI` (`$CB`) et le relâchement d'IRQ sur `$FFFF`. Recouvre le point « tick/IRQ trame » de T-09. | **(a) DONE neo 0.8.0** (`irq.cpp`, `tick.cpp`, boucle bus 11 nop, DVI vsync, `neo` : IRQ + `WAI` corrigé, tests `frameirq`/`irqtick`), **à valider carte avec prudence** (`trinity-0.8.0-irq-USB.uf2`) ; **(b) DONE neo 0.8.1** (`kernel/rtos.asm`, démos rtos/rtos_idle identiques au golden du fork) ; carte : (a) puis (b) à valider |
| T-15 | P1 | **NeoDOS remplace NeoBASIC comme environnement résident** (carte 2026-09-21 : NeoDOS 0.14.0 à l'invite `A:\>` sur 0.8.3, `NEOBASIC.NEO` lance le BASIC) (décision bmarty 2026-09-20). Le firmware n'embarque plus `basic_binary.h` (dépôt Neo6502Basic) mais `neodos_binary.h`, généré à la construction par `kernel/scripts/hconvert.py` depuis `build/neodos.bin` du dépôt Neo6502Msdos (`NEODOSDIR`, défaut `../Neo6502Msdos/` ; image brute `$C000-$E854`, 10 325 o, NeoDOS 0.12.0). `MEMInitialiseMemory` et `1,3` chargent NeoDOS depuis la flash et pointent `jmp (0)` sur `$C000` ; le premier `1,3` applique toujours le choix du menu `boot/` (défaut affiché : `1 NeoDOS`). La copie racine `neobasic.bin` (T-03) est retirée : NeoBASIC se lance par `boot/neobasic.bin` (= `bin/basic.bin` de Neo6502Basic, image `$800`) et `boot/auto.txt`. Le firmware, `neo` et le noyau ne dépendent plus de Neo6502Basic (`BASICDIR` ne sert qu'à `examples/` et `release/`). Critères : `neo` sans `boot/` → NeoDOS exécute `AUTOEXEC.BAT` (vérifié : `RESULT.TXT` écrit, `VER` = NeoDOS 0.12.0 / firmware 0.3.1) ; `boot/neobasic.bin` + `auto.txt` → NeoBASIC démarre (vérifié : cherche `autostart.bas`). UF2 360 960 o (−18 944 o), RAM inchangée. Conséquence NeoDOS : `EXIT` (1,3 + `jmp (0)`) relance NeoDOS, plus NeoBASIC — à consigner dans Neo6502Msdos. | **DONE, validé carte** 2026-09-20 (avec 0.4.1 : menu `boot/`, `auto neobasic.bin`, NeoBASIC depuis la clé) ; NeoDOS lui-même à l'invite : à confirmer. **0.7.0** : image NeoDOS 0.14.0 chargée en **`$B800`** (`hconvert … B800`, la base de NeoDOS a changé — ADR-004 Neo6502Msdos) ; à valider carte |
| T-16 | P1 | **NeoBASIC en mode 1 Hercules** (signalement bmarty 2026-09-20 : « le BASIC a un problème avec le MDA » ; précisé ensuite : sur carte, après `vmode 1`, **écran noir, signal présent, sans activité** ; `mda.neo6502` fonctionne). Exploration dans `neo` : deux défauts corrigés en **0.4.1** — `cls` (encre 7 codée en dur dans la mémoire console → soulignés parasites après défilement) et le codage F-52 des attributs (encres NeoBASIC 2, 3, 6, 7 soulignées/grasses, 11 clignotante → `list` illisible, constantes invisibles une demi-seconde sur deux) remplacé par le **codage IBM MDA** (`papier << 4 \| encre` : encre 0 éteint, 1 souligné, 2-7 normal, 8-15 gras ; papier 1-7 inverse, 8-15 clignotant ; curseur toujours inversé). **Écran noir carte expliqué** (réponses bmarty au protocole : UF2 0.3.0 ; `vmode 0` à l'aveugle ramène l'image ; idem 0.3.0 ; rien en attendant) et reproduit dans `neo` avec la console 0.3.0 (`vmode 1` au prompt → 0 pixel allumé) : après `vmode`, NeoBASIC n'imprime rien et n'affiche que le curseur, dessiné par XOR avec l'encre courante — le vert du prompt (2) a le bit 0 nul → aucun pixel inversé en 1 bpp ; `mda.neo6502` n'a pas de curseur. Corrigé en 0.4.1 (curseur toujours inversé en 1 bpp), vérifié `neo`. | **DONE, validé carte** 2026-09-20 (0.4.1 + `boot/neobasic.bin` : curseur visible, `list` lisible, `vmode 0` OK) ; NeoBASIC `list.asm` : espaces après le numéro en blanc |
| T-18 | P2 | **Reprises du fork pour NeoDOS et Phosphoneo** (demande bmarty 2026-09-20 « go 1 ») : volumes 3,24-3,26 (F-102), `3,27` File Read Paged (F-16), date/heure 1,20-1,21 (F-14) + **horodatage FAT sur la clé USB** (`get_fattime` depuis l'horloge du firmware, `FF_FS_NORTC = 0`). Livré en **0.6.0**, tests `make test-api` (readpaged, datetime, volumes) dans `neo`. Carte : à valider (plusieurs clés = plusieurs volumes ; PCF8563 jamais vu sur carte ; `DATE`/`TIME`/`VOL`/`B:` de NeoDOS ; fichier créé par NeoDOS daté). Reste : dates dans 3,18/3,16 (mémo NeoDOS, point 3) ; Phosphoneo : `clock.h` est de retour, il lui manque encore les volumes côté hôte (`neo_host.cpp` suit le fork). | **DONE neo** 0.6.0, à valider carte |
| T-19 | P3 | **`neo` : crochets de test** (point 2 de la liste des reprises, demande bmarty 2026-09-21) : `cycles:` `shot:` `text:` `keys:` `mouse:` du fork, sans IRQ/WAI (T-14) ; `hid:C:LISTE` ajouté en 0.10.6 (scancodes HID bruts, pour les touches sans ASCII : verrouillages, pavé numérique). | **DONE** 0.6.1 (events.asm automatisé), `hid:` 0.10.6 |
| T-20 | P3 | **Latin-1 / locale FR, `2,21` police console, `2,20` écho** (point 3 des reprises) : F-17, F-95, F-92 du fork. | **DONE neo** 0.7.1, à valider carte (clavier AZERTY réel : `é è ç à ù ² ° £ § µ`, touches mortes) |
| T-21 | P3 | **Titres de fenêtre > 31 caractères** (demande d'un autre projet, 2026-09-21) : le Window Manager lit le titre en place en RAM 6502 (plus de `WM_TITLE_MAX`, −248 o de RAM). | **DONE** 0.8.1 |
| T-22 | P3 | **Volume par canal son** (F-12 du fork) : `8,9`/`8,10`, rampes. | **DONE neo** 0.8.2 |
| T-24 | P1 | **Volumes sur carte** (retour carte bmarty 2026-09-21 sur 0.8.1 : `!:\>`, `A:` refusé, `b:1:\>`) : clé montée à son adresse USB → première clé = lecteur `0:` (`A:`) ; `3,26` non initialisé avant montage → volume courant suivi ; `3,23` sans préfixe `n:`. | 0.8.2 : `A:\>` OK sur carte mais **gel** (drapeau occupé du diskio indexé par lecteur vs adresse USB) → **0.8.3 validée carte** (2026-09-21 : `A:\>`, clavier, BASIC via `NEOBASIC.NEO` ou menu) |
| T-25 | P2 | **Heure par le modem Pico W** (validé carte 2026-09-22 : `1,23` règle l'heure) (demande bmarty 2026-09-21, `DATE` = 1970 sans RTC) : `1,23` + synchro automatique au premier `1,20` non réglé ; fuseau = `tz` du modem (heures entières, sans heure d'été). Suite possible : T-26 règle européenne été/hiver dans le firmware. | 0.9.8 : **synchro automatique retirée** (gel carte au démarrage clé présente, 0.9.5-0.9.7, bissection A) ; `1,23` explicite seul ; carte 2026-09-22 : `clocksync.neo6502` → `SYNC 00`, **`1,23` fonctionne hors démarrage** (le gel tenait au contexte de démarrage, sonde `1,20` pendant l'énumération USB) ; outil `tests/api/outils/tzparis.asm` (`1,24` Europe/Paris + `1,23`) ; à faire côté NeoDOS : `DATE`/`TIME` → `1,23` quand la source est 0 |
| T-26 | P2 | **Fuseaux horaires dans le firmware** (demande bmarty 2026-09-21) : `1,24`/`1,25`, ~120 zones IANA + décalages fixes, règles été UE/AN/AU/NZ, horloge en UTC, réglage dans un **secteur de réglages en flash** (`settings.cpp`, réutilisable : locale…). | **DONE, validé carte** 2026-09-22 via `TZPARIS.NEO` (1,24 Europe/Paris sauvé en flash + 1,23) |
| T-27 | P3 | ~~Curseur souris automatique au mouvement~~ — livré en 0.9.1 puis **annulé en 0.9.3** (décision bmarty 2026-09-21 : « c'est au programme de faire apparaître la souris », `11,2`). | ANNULÉE |
| T-28 | P2 | **Échap au démarrage** (retour carte bmarty 2026-09-21) : attendre le montage du clavier USB (≤ 8 s) avant la fenêtre de 3 s. | **DONE** 0.9.4, à valider carte |
| T-29 | P3 | **Curseur souris en mode 1** (demande bmarty 2026-09-22) : superposition 1 bpp dans le callback DVI. | **DONE** 0.9.9, à valider carte |
| T-30 | P1 | **Écritures flash et périphériques USB** (retours carte bmarty 2026-09-22) : 0.9.9 démontait le DVI (écran noir), 0.9.10 gardait l'image mais laissait le clavier muet ~10 s ; 0.9.11 = lockout multicore + `HWUSBRecover` (300 ms de `tuh_task`). | **DONE, validé carte** 2026-09-22 (`TZPARIS.NEO` : écriture flash, écran conservé, clavier immédiat) ; `banks.neo6502` (2 × 4 Ko) reste à voir |
| T-31 | P1 | **Clavier muet pendant les accès disque** (carte 2026-09-22) : en **NeoDOS**, Tab répété rapidement (complétion = rafale d'accès FatFs) bloque le clavier ; en **NeoBASIC le même geste ne le reproduit pas**. Bissection : la variante B (0.9.11 sans lockout multicore ni écriture flash) reproduit → ni le lockout (T-30) ni la flash ne sont en cause. Piste : sur RP2040 TinyUSB 0.21 partage l'endpoint matériel EPX entre le bulk MSC et l'interrupt HID ; pendant `wait_for_disk_io` (qui tourne dans `tuh_task`) une frappe peut perdre l'endpoint clavier (défaut connu, tinyusb #3533/#1261, déjà vu en 0.1.0). Traits rouges = lignes DVI en retard, connues depuis 0.3.0, sans rapport. À faire : (a) mesurer (compteur de reports HID perdus, `2,20` écho série) ; (b) essayer `HWUSBRecover()` après chaque transfert MSC ; (c) côté NeoDOS, limiter les accès disque de la complétion (cache du catalogue courant). | TODO |
| T-32 | P1 | **Boot déterministe** (ADR-0001, demande bmarty 2026-09-22) : phases P0-P3 et **barrière de calme USB** (300 ms sans événement d'énumération, plancher 500 ms, plafond 5 s) à la place de `msc_inquiry_complete` + 2 s. | **DONE** 0.10.0 (phases P0-P3, `usbsettle.cpp`), à valider carte |
| T-33 | P1 | **Watchdog de boot et journal** (ADR-0001) : watchdog 8 s armé avant P0, rafraîchi par `DSPSync` ; phase et raison du reset dans les registres scratch, affichées (`boot: watchdog @P1`). | TODO (b) |
| T-34 | P2 | **Attentes bornées** (ADR-0001) : `wait_for_disk_io` avec délai maximal ; règle générale, aucune boucle d'attente sans borne. | TODO (c) |
| T-35 | P2 | **Politique de démarrage en flash** (ADR-0001) : choix automatique dans le secteur de réglages, `auto.txt` en repli ; `boot/` = catalogue. | TODO (d) |
| T-36 | P2 | **Contrats `1,0` / `1,3`** (ADR-0001) : `1,0` = reset logiciel (plus de `tusb_init` à chaud), application du choix de démarrage séparée de `1,3` ; vérification des images chargées. | TODO (e) |
| T-37 | P3 | **Tests de boot** (ADR-0001) : `tests/boot/` dans `neo` (sans clé, clé lente, sans clavier, `auto.txt` invalide, image corrompue). | TODO (f) |
| T-66 | P2 | **Les logos restent visibles au démarrage** (idée bmarty 2026-09-24 : « ne peux-tu pas bloquer le DVI après les logos, attendre 3 secondes, puis afficher tout le reste »). Les messages d'énumération s'écrivaient dans la même mémoire que les logos et, passé la ligne 29, faisaient **défiler** l'écran : plus il y avait de périphériques, plus vite les logos disparaissaient. Réorganisation des phases : **P0** dessine les logos puis passe la console en mode silencieux (`CONSetQuiet`) ; **P1** découvre l'USB **sans rien écrire**, avec un **plancher de 3 s** (`USB_FLOOR`) ; **P2** rétablit la console et affiche tout d'un coup — bannière, bilan de la barrière (`USBReport`, différé), stockage, fuseau, menu `boot/`. L'attente ne coûte rien : la barrière de T-32 patientait déjà là, et l'énumération se fait pendant que les logos sont à l'écran. | **DONE, validé carte** 2026-09-24 : les logos tiennent leurs 3 s, et **plus aucun trait rouge au démarrage** — la phase bruyante ne se superpose plus à l'énumération, ce qui achève T-58 |
| T-65 | P1 | **Le rapport de sortie HID, refait sans danger — et il ne sert pas qu'aux diodes** (retour carte bmarty 2026-09-24 : « le num lock ne fonctionne pas », « la touche **l** devrait afficher le chiffre **3** »). Le clavier est un **1A2C:0B2A** (relevé sur le PC : deux interfaces HID, la 0 en clavier boot) doté d'un **pavé numérique intégré** — le type compact où `j k l` donnent `1 2 3`. Ce basculement est fait **par le clavier**, et il ne le fait **que si l'hôte lui envoie le rapport de sortie HID** disant que Num Lock est allumé : celui-là même qui allume les diodes. En retirant T-40 en 0.10.12 (elle détruisait la pile USB, bissection T-47), j'ai donc aussi supprimé le seul moyen pour ce clavier d'apprendre l'état du verrou — d'où un pavé intégré qui ne bascule jamais. Refait comme le backlog le prévoyait : `KBDLockLEDUpdate` **enregistre** la demande, et `KBDSync` l'émet **entre deux `tuh_task()`**, jamais depuis un callback. | **DONE, validé carte** 2026-09-24 : le pavé intégré bascule enfin (`j k l` = `1 2 3`), et **le jeu tourne** — l'émission hors callback ne reproduit pas le désastre de T-40 |
| T-64 | P2 | **Le pavé numérique donne toujours ses chiffres** (décision bmarty 2026-09-24, revient sur T-41) : « les touches marquées 1234567890 doivent afficher le chiffre, que L soit à 0 ou à 1 ». Num Lock garde son état et sa lecture par `2,23`, mais ne change plus ce que produisent les touches ; le tableau `padDown` de T-41, qui mémorisait le mappage de l'appui, disparaît (−16 o). Les flèches et Origine/Fin du bloc principal ne sont pas concernées : ce sont des touches distinctes. Test `locks` mis à jour, et il vide désormais la file clavier avant de mesurer — un résidu s'y glissait et décalait tout. | **DONE, validé carte** 2026-09-24 |
| T-62 | P1 | **Relecture terminée : tables API (2 492 lignes) et derniers fichiers.** Un défaut de plus : `3,23 Get Current Working Directory` écrivait à une adresse **et** sur une longueur toutes deux fournies par le programme, sans vérifier leur somme — près du haut de la mémoire, l'écriture la dépassait. Relus **sans défaut** : les sept tables de la toolbox (32-38), les groupes 4 (maths), 6 (sprites), 7 (manettes), 8 (son), 9 (tortue), 11 (souris), 13 (éditeur) ; `QDSetPattern` et les chaînes de QuickDraw bornent déjà (`strAddr + len > 0xFF00`) ; `CDCReadBlock`/`CDCWriteBlock` bornent déjà (`addr + max > 0x10000`) ; `1,24` tronque le nom de fuseau ; `SFXPlay` et la tortue bornent leurs identifiants. **Bilan des quatre lots : 19 défauts corrigés**, dont une faute matérielle (`2,10` ligne 0), un gel complet de la machine (lecture série sans octet) et une écriture illimitée (blitter `12,3`). | **DONE** 0.12.3 |
| T-61 | P0 | **Relecture, lot 3 (blitter, commandes MOS)** : (1) **le blitter complexe (`12,3`) ne vérifiait que la PREMIÈRE adresse de chaque zone.** `width` est sur 16 bits, donc une seule ligne pouvait copier 64 Ko ; et `height` (255) fois `stride` (65535) faisait avancer le pointeur de près de **16 Mo**. Or ces champs sont lus dans une structure que le programme 6502 écrit lui-même : c'était une **écriture illimitée** dans ce qui suit la zone, alors que `BLTSimpleCopy`, juste à côté, vérifie début **et** fin. Corrigé : chaque ligne est rejetée si elle ne tient pas entièrement dans sa zone (`_BLTAreaEnd`, `_BLTLineFits`). C'est probablement le défaut le plus grave de la relecture, et le blitter est très utilisé (NeoLegacy s'en sert pour tous ses décors). (2) `mos.cpp` : `paramData[256]`, mais 255 caractères de commande plus un octet de longueur et un terminateur par paramètre (quatre au plus) font 263 octets — sept de trop. Relus sans défaut : `sprites_xor` (bornes d'écran vérifiées), `ellipse`, `efla`, `cursor`, `logo` (données internes, pas d'entrée externe), `locale`, `clock`, `timezone`, `banks`, `usbsettle`. `make test-api` 16/16, `make test-toolbox` 10/10. | **DONE** 0.12.2 |
| T-60 | P0 | **Relecture, lot 2 (graphismes, console, éditeur, lien série, tables API)** : huit défauts de plus, même profil que T-59. (1) `GFXDrawImage` ne testait pas le `-1` de `GFXFindImage` et lisait **avant** `gfxObjectMemory`. (2) `GFXFindImage` calcule son adresse à partir de compteurs lus **dans le fichier de graphismes** : au maximum elle atteignait 97 Ko pour une zone de 32 Ko. (3) `TMPGetTileRowAddress` : avec 255 tuiles, la base vaut déjà 32 768 pour 32 768 octets, avant même le décalage de ligne. (4) `TMPSelectTileMap` : la carte est lue à `mapData + 3 + y*width + x`, `width` et `height` venant de la carte elle-même — placée haut, elle sortait des 64 Ko. (5) **`CONInsertLine` était doublement cassée**, à la différence de sa symétrique `CONDeleteLine` : la boucle faisait varier `y1` mais copiait toujours `y-1` vers `y`, donc **rien ne se décalait** ; et `CONCopy` prenant des `uint16_t`, la ligne 0 donnait `y-1` = 65535, soit une lecture **plusieurs mégaoctets** plus loin — adresse invalide sur RP2040, donc faute matérielle. Atteignable par `2,10` sur la ligne 0. (6) Éditeur : ligne lue et réécrite à une adresse fournie par le 6502, jusqu'à 255 octets au-delà de la mémoire. (7) **Lien série** : `sBuffer[256]` écrit jusqu'à l'indice 256, `fileName[32]` rempli avec une longueur allant à 255, adresse graphique 16 bits pointant hors d'une zone de 32 Ko, et transfert sans borne de fin. (8) **Groupe 10** : les six transferts par blocs (I2C, SPI, UART) recevaient adresse **et** longueur sur 16 bits, sans vérifier leur somme. `make test-api` 16/16, `make test-toolbox` 10/10. | **DONE** 0.12.1 |
| T-59 | P0 | **Relecture du code (demande bmarty 2026-09-23) : huit écritures ou lectures hors limites déclenchables depuis un programme 6502.** Toutes du même profil que T-48 — ce qu'elles écrasent dépend de la disposition des variables, donc du binaire. (1) `fileinterface.cpp` `FIOReadFile` : `cpuMemory[loadAddress+i]` sans borne, `loadAddress` et `loadSize` étant 16 bits chacun, l'écriture allait **jusqu'à 64 Ko au-delà** de la mémoire 6502 ; même chose pour `gfxObjectMemory`. (2) Même fichier, bloc de commentaire : garde **inversée** (`>` au lieu de `<`), le pointeur ne commençait à avancer qu'après le 32ᵉ caractère et **ne s'arrêtait jamais** — un commentaire `.NEO` de plus de 64 caractères écrivait sans limite. (3) `maths.cpp` : `sprintf` d'un `%f` (une quarantaine de caractères) à une adresse 6502 quelconque, sans borne. (4) `console.cpp` lecture de ligne : `cpuMemory[addr+256]` sans borne. (5) `fileimplementation.cpp` : lecture vers `gfxObjectMemory` **non bornée** (32 Ko de débordement) et `uint16_t(0x10000-address)` qui **vaut 0 à l'adresse 0**, donc transfert silencieusement vide. (6) `sndmanager.cpp` : `>` au lieu de `>=`, `channel[4]` lu hors tableau par `8,3`. (7) `serial.cpp` `SERReadByte` : **attente infinie** — un programme qui lit le port série sans octet disponible **fige toute la machine**, alors que l'historique du fichier prétend qu'un délai a été ajouté en 2024 (T-34). (8) Émulateur `neo` **et** Phosphoneo : mêmes lectures de fichier sans aucune borne — un modèle de référence qui se corrompt masque sur PC ce qui casse sur carte. | **DONE** 0.12.0 |
| T-58 | P1 | **RÉSOLU en 0.12.5 : la conversion de palette se fait par mots de 32 bits.** Ce qui affamait core 1 pendant un transfert n'était pas le nombre de cycles mais la **bande passante mémoire** — cinq tampons TMDS n'avaient rien changé, et donner la priorité au DMA avait **empiré** les traits. Or le callback de ligne, exécuté **en interruption sur le cœur de l'encodeur**, faisait pour chaque ligne de 320 pixels : 320 lectures d'octets, 320 lectures de table et 320 écritures de 16 bits. En traitant quatre pixels par mot, cela tombe à **80 lectures et 160 écritures** — moitié moins de transactions, exactement là où ça manquait. Alignement garanti à la déclaration, donc aucun test à l'exécution. **Carte 2026-09-24 : plus aucun trait.** Le résidu du démarrage a disparu à son tour avec T-66, qui sépare l'énumération silencieuse de la phase texte. Coût : ~120 o de code en RAM, d'où `RAM_LIMIT` porté de 230 000 à 231 000 (marge vérifiée : 31 Ko de tas restants). **Ancien libellé ci-dessous.** | **DONE** 0.12.5, validé carte |
| T-58b | P2 | (mesures) **un trait rouge = une ligne en retard** (mesure carte bmarty 2026-09-23 avec 0.11.6) : `DVI` passe de `89` à `8A` **exactement** quand un trait apparaît, `SY` de 17 à 18. L'encodeur (core 1) manque donc ponctuellement de temps pendant les accès disque, et le trait est la ligne qu'il n'a pas livrée. Ce n'est **pas** la contention flash : tout PicoDVI est déjà en RAM (`__not_in_flash_func`, sections `.scratch_x`/`.scratch_y`), comme notre callback et notre boucle d'encodage. Reste la **contention mémoire** — le DMA USB écrit les secteurs, core 0 les recopie, core 1 lit la mémoire vidéo au même moment — confirmée a contrario par 0.11.5 : donner la priorité au DMA a **aggravé** les traits. Pistes, par coût croissant : (a) `DVI_N_TMDS_BUFFERS` passe de 3 à 4 pour absorber les à-coups (+3,8 Ko de RAM en mode 0 : impossible tel quel, il reste 32 Ko de tas mais la marge sous `RAM_LIMIT` n'est que de 228 o) ; (b) alléger le callback de ligne, qui fait 320 conversions de palette **en interruption** sur le cœur de l'encodeur ; (c) répartir les tampons entre bancs SRAM pour réduire la contention. Mesure de référence : **le nombre d'épisodes pendant N Tab**, désormais fiable. | **TODO** — essai 0.12.4 : passer de 3 à 5 tampons TMDS n'a **rien changé** (traits et compteur inchangés). L'encodeur ne manque pas d'avance mais de **débit** : une réserve de tampons s'épuise au lieu d'absorber. Revenu au défaut. Reste (b) alléger le callback de ligne, (c) les bancs SRAM |
| T-57 | P0 | **`5,40` ne mesurait rien : `late_scanline_ctr` est un état, pas un total** (2026-09-23) — `dvi.c` de PicoDVI l'incrémente quand une ligne manque et le **décrémente dès que le pipeline rattrape**. Le lire depuis `DSPSync`, 95 fois par seconde, le retrouvait presque toujours retombé à zéro : c'est pourquoi « `DVI` = 0 » a blanchi l'affichage toute la journée, **à tort**, et a fait écarter la piste des lignes en retard. Corrigé : le **callback de ligne** (core 1, toutes les 31 µs) échantillonne et cumule dans `lateTotal`, que `5,40` renvoie désormais. Leçon : vérifier ce que compte un compteur avant de conclure de son zéro. | **DONE** 0.11.6, à mesurer carte |
| T-56 | P1 | **Traits rouges : priorité du DMA sur les cœurs** (mesure `VRAMCHK` 2026-09-23 : **VRAM saine pendant que les traits défilent**) — le défaut est donc **après la mémoire**, dans le chemin vers l'écran, et **sans rapport avec la panne des programmes** (deux affaires distinctes, comme bmarty le pressentait). Les trois canaux TMDS sont servis par **trois canaux DMA séparés** dont PicoDVI ne demande **jamais** la priorité, alors que son propre commentaire dit « we really don't want the FIFOs to bottom out ». Pendant la lecture d'un secteur, core 0 copie à pleine vitesse et prive ces canaux : une lane se vide et peint un trait, le rouge étant le troisième. Correction : `bus_ctrl_hw->priority = DMA_R | DMA_W` dans `DVIStart` — une ligne, aucun coût mémoire, et ce que la fiche technique recommande pour du DMA temps réel. Explique aussi pourquoi le mode 1 est épargné : il n'a qu'un encodage et ses lanes éteintes lisent un canal noir constant. | **ANNULÉE en 0.11.6** : sur carte les traits sont devenus **bien pires** (bmarty 2026-09-23). Information utile malgré tout — c'est **core 1, l'encodeur**, qui est affamé, pas le DMA de l'affichage : lui prendre de la bande passante aggrave |
| T-55 | P0 | **Outil `VRAMCHK` : la mémoire vidéo est-elle corrompue, ou est-ce le signal ?** (2026-09-23, les traits rouges survivent à T-53 et T-54) : efface l'écran, fait huit catalogues complets du répertoire courant (3,17/3,18/3,19 — ce que fait la complétion sur Tab), puis **relit toute la moitié basse de la VRAM** par `12,2` et signale le premier octet non nul, avec son offset et sa valeur. Le compte-rendu reste sur une ligne fixe de la moitié haute, pour ne jamais salir la zone scrutée (curseur caché après le CLS, qui le rallume). Verdict sans interprétation : **octet trouvé = corruption mémoire** (et son offset dit où elle tombe) ; **rien trouvé alors que les traits sont visibles = la VRAM est saine**, le défaut est dans l'encodage ou la transmission du signal — voire électrique, les pics de consommation USB pendant un transfert. | **DONE, mesuré** 2026-09-23 : **VRAM saine** pendant que les traits défilent → la mémoire vidéo est hors de cause, voir T-56 |
| T-54 | P0 | **Débordement des tableaux MSC indexés par l'adresse USB** (trouvé 2026-09-23 en suivant les traits rouges) — **explique T-48, T-38 et probablement T-31**. `usb_storage.cpp` indexait `msc_fatfs_volumes[]` et `msc_volume_busy[]` (taille `CFG_TUH_DEVICE_MAX` = **5** avec hub) **par l'adresse USB elle-même**, or TinyUSB distribue les adresses **1 à 5** — l'adresse 0 est celle de l'énumération. Une clé qui obtient la **dernière** adresse écrit donc **une case au-delà** des deux tableaux : un octet parasite pour le drapeau, et **une structure `FATFS` entière** (des centaines d'octets) par-dessus ce que l'éditeur de liens a placé ensuite — **à chaque accès disque**. Avec hub + clavier + souris + modem + clé, cette adresse est atteinte en pratique. Ce que le débordement écrase dépend du binaire : d'où un comportement carte qui semblait aléatoire (**T-48**), et des **traits rouges** quand il tombe dans `graphicsMemory` (la couleur 1 de la palette par défaut est `255,0,77`). Cohérent avec toutes les mesures : `UN`, `OV`, `DVI` à 0, aucune anomalie de bus ni d'affichage — juste de la mémoire écrasée. Correction : emplacement = adresse − 1, et **toutes** les indexations bornées (`mscSlot`). | **DONE** 0.11.4, à valider carte |
| T-53 | P1 | **Traits rouges : quatre tampons de ligne au lieu de deux** (reproducteur bmarty 2026-09-23 : **Tab dans NeoDOS les déclenche à coup sûr** — la complétion parcourt le répertoire, c'est la rafale FatFs la plus dense que le clavier sache produire). Mécanisme : en mode 0 l'encodeur lit **trois fois** le même tampon de ligne, un passage par canal TMDS, **rouge en dernier** ; le callback de ligne tourne en interruption **sur le même cœur** et n'avait que **deux** tampons à alterner. Quand core 0 sature la mémoire pour FatFs, l'encodeur décroche, le callback revient sur le tampon qu'il est en train de lire, et seule la troisième passe voit les données neuves : bleu et vert justes, **rouge faux**. `late_scanline_ctr` reste à 0 parce que le tampon TMDS est publié à l'heure — c'est sa **source** qui a changé sous lui. Le mode 1 n'a qu'un encodage par ligne, donc pas de dernier canal à rater : cohérent avec « jamais en mode 1 ». Correction : **4 tampons**, dimensionnés pour le plus large mode **couleur** (320, mode 0) et non pour le plus large mode (720, monochrome, qui passe par `monoLine`) — quatre tampons coûtent ainsi **moins** que les deux anciens (−208 o, marge sous `RAM_LIMIT` portée de 80 à 264 o). Validation : Tab ne doit plus produire de traits. | **DONE** 0.11.3, à valider carte |
| T-38 | P2 | **Traits rouges — mesures carte du 2026-09-23** : `5,40` (`late_scanline_ctr` de PicoDVI) reste à **0** au repos, à la frappe **et pendant les `DIR`**, alors que les traits apparaissent surtout à ce moment-là. Ce ne sont donc **pas des lignes perdues** : le pipeline livre chaque ligne à l'heure, c'est son **contenu** qui est faux. Précision bmarty : ils n'apparaissent **qu'en mode 0**, jamais en mode 1. Or en mode 0 l'encodeur traite les trois canaux l'un après l'autre et **le rouge est le dernier** (`tmds_encode_data_channel_16bpp` ×3) ; le mode 1 ne fait qu'un encodage par ligne, sans « dernier canal » à rater. Un trait rouge est donc la signature d'un encodage dont le troisième canal n'a pas abouti, ou d'un tampon TMDS touché juste avant la fin — pendant que core 0 martèle la flash pour FatFs et TinyUSB. Piste à instrumenter : compteur de préemption du callback de ligne sur l'encodeur, ou plus de tampons (coût 3 Ko de RAM en mode 0 : il n'en reste que 80 octets sous `RAM_LIMIT`). **Ancien libellé ci-dessous.** | WIP, mesuré |
| T-38b | P2 | (ancien) **Lignes DVI en retard** (traits colorés, carte 2026-09-22) : 0.10.2 (core 1 sans IRQ du lockout) puis 0.10.3 (callback de ligne sans code flash, compteur `5,40` + `LATE.NEO`). Reste à mesurer : combien de lignes par seconde à la frappe, et d'où vient le reste (contention XIP de core 0, `queue_*` de pico_util en flash, repeint console). | WIP |
| T-39 | P3 | **Touches multimédia des claviers USB** (demande bmarty 2026-09-22, `No driver found for 1A2C0B2A`) : seconde interface HID (Consumer + System Control) revendiquée par le clavier au lieu du gestionnaire de manettes ; `2,22 Get Media Key`. | **DONE** 0.10.4, à valider carte (plus de message, `2,22` renvoie les touches) |
| T-40 | P2 | **LED Num/Caps/Scroll Lock** (retour carte bmarty 2026-09-22 : elles ne s'allumaient pas) : l'amont n'envoyait pas le rapport de sortie HID ; état tenu par le firmware, envoyé au clavier, lisible par `2,23`. | **RETIRÉE en 0.10.12** : l'envoi du rapport (un transfert de contrôle) depuis `tuh_hid_mount_cb` et depuis le callback de rapport — donc depuis `tuh_task` — détruisait la pile USB hôte sur carte ; **depuis 0.10.5, tout programme mourait peu après son lancement** (bissection bmarty 2026-09-23, voir T-47). L'état et `2,23` restent (T-41 en dépend), les diodes non. À refaire autrement : demande mise en file et émise depuis la boucle principale, hors de tout callback (terrain de T-31, EPX partagé sur RP2040) |
| T-41 | P1 | **Num Lock et Caps Lock rendent leur service** (retour carte bmarty 2026-09-22 sur 0.10.5 : « num lock ou caps lock ne rendent pas leur service ») : la 0.10.5 n'allumait que les diodes, l'état n'agissait sur rien. L'amont ne consultait jamais les verrous (`KBDMapToASCII` ne regarde que Shift) et remappait le pavé numérique sur les chiffres **sans condition** (`usbdriver.cpp`), en laissant `/ * - + Entrée .` sans aucune correspondance — touches mortes. Décisions bmarty 2026-09-22 : **Caps Lock = lettres seulement** (inverse la casse de `a-z`/`A-Z` après la locale, les chiffres et symboles ne bougent pas — sans effet de bord sur la table FR) ; **Num Lock éteint au démarrage**, allumé = chiffres et `.`, éteint = navigation imprimée sur les touches (Fin, ↓, PgSuiv, ←, —, →, Origine, ↑, PgPréc, Inser, Suppr). L'état vit désormais dans `keyboard.cpp` (commun firmware/`neo`, donc testable) ; `usbdriver.cpp` ne fait plus que transmettre les scancodes bruts et porter l'état aux diodes (`KBDLockLEDUpdate`). Le mappage retenu à l'appui est rejoué au relâchement (`padDown`), pour qu'une bascule de Num Lock touche enfoncée ne laisse pas de touche collée. Test `tests/api/locks.asm` (crochet `hid:`, T-19). | **DONE** 0.10.6, à valider carte (`trinity-0.10.6-locks-USB.uf2`) |
| T-42 | P2 | **Curseur souris aux bords de l'écran** (trouvé en cherchant la régression de NeoLegacy sur carte, 2026-09-22) : `RNDCursorUpdate` (T-32c, 0.10.3) calculait la position en **non signé** — `x -= xHit` passe à ~65530 dès que le pointeur approche le bord gauche ou haut avec un point chaud non nul, et `w = xGSize - x` donnait alors 1 726 au lieu d'une largeur tronquée. Les gardes du callback rejetaient ces valeurs : le curseur **disparaissait** au lieu d'être découpé (aucune corruption mémoire : les tampons de ligne ont de la marge). Corrigé en 0.10.7 : calcul signé, découpe des quatre bords avec `skipX`/`skipY` (colonnes et lignes de l'image sautées), curseur désactivé s'il est entièrement hors écran ; modes 0 et 1. Non reproductible sous `neo` (ce fichier n'est compilé que pour la carte) : vérifié par compilation, **à valider carte**. | **DONE** 0.10.7, à valider carte |
| T-43 | P2 | **État du curseur publié d'un bloc** (revue du code 0.10.x, 2026-09-22) : `RNDCursorUpdate` (0.10.3) écrivait ses sept champs un par un dans des variables `volatile` que core 1 recopiait au début de trame — core 1 pouvait donc lire un **mélange de deux états** (position neuve avec largeur ancienne, ou `enabled` avec un pointeur d'image pas encore écrit), et rien ne protégeait contre une image nulle. 0.10.8 : deux emplacements (`cursorSlot[2]`), core 0 remplit celui que core 1 ne lit pas puis publie l'index par une écriture d'un octet (atomique sur M0+, précédée d'un `__dmb()`) ; le curseur n'est jamais dessiné depuis une image nulle. Comme T-42 : pas de test automatique possible (fichier carte), à valider carte. | **DONE** 0.10.8, à valider carte |
| T-44 | P1 | **Image du curseur lue en flash depuis le callback DVI** (revue 2026-09-22, piste pour la régression de NeoLegacy sur carte) : 0.10.3 a sorti du callback de ligne les *appels de fonction* en flash (T-32c), mais **pas les données** — `CURGetCurrent` renvoie un pointeur dans `cursor_data`, qui est `const` donc en flash, et le callback y lisait un octet par pixel de curseur (jusqu'à 16 × 16 par trame) sur core 1, pendant que core 0 martèle la flash : exactement la contention XIP que T-38 traque. Invisible tant que le curseur est caché (il l'est au reset depuis 0.9.3, T-27) ; **NeoLegacy 0.29.1 est le premier programme à le rendre visible** (`11,2`), et c'est là que la carte perd l'image. 0.10.9 : `RNDCursorUpdate` copie l'image en RAM (`cursorPixels`, 256 o, seulement quand le curseur change) et le callback ne lit plus que de la RAM. Vérifiable sur carte : `5,40`/`LATE.NEO` doivent montrer moins de lignes en retard, curseur visible, qu'en 0.10.8. **Hypothèse, non prouvée** : à confirmer carte. | **DONE** 0.10.9, à valider carte |
| T-45 | P2 | **Outils carte reconstruits automatiquement** (retour carte bmarty 2026-09-23 : « late plante à nouveau ») : la 0.10.5 avait retiré de `late.asm` son journal RAM **et la routine `cr`** qu'il appelle encore — l'assemblage échouait, `LATE.NEO` n'a jamais été reconstruit, la clé gardait la version qui plante, et la note de version l'affirmait corrigé. 0.10.10 : `cr` rétablie et cible **`make outils`** (`tests/api/outils/*.asm` → `~/neo-carte/cle-usb/*.NEO`). Leçon : un outil carte sans cible de construction finit périmé sans que rien ne le signale. | **DONE** 0.10.10 |
| T-52 | P0 | **Transactions fantômes : la boucle du bus lit le FIFO PIO sans vérifier qu'il est plein** (analyse 2026-09-23, après la mesure carte) — **hypothèse forte pour T-48**. `processor_pio.cpp` appelle `pio_sm_get(pio1,0)` pour l'adresse (hors du cas `consecutive_writes >= 3`) **et** pour la donnée d'une écriture ; or `pio_sm_get` du SDK est `return pio->rxf[sm];`, **sans attendre que la file contienne quelque chose**. Si le firmware arrive avant la machine PIO, il lit un mot périmé et écrit alors `cpuMemory[adresse fantôme] = donnée fantôme` : **le programme 6502 est corrompu pendant que le firmware continue** — ce qui explique que Ctrl+Alt+AltGr redémarre encore la carte alors que l'écran est perdu (mesure bmarty 2026-09-23), là où un blocage du firmware n'y répondrait pas. Et comme cela dépend de la **vitesse relative** du firmware et du PIO, un binaire dont le code chaud tombe mieux en cache lit plus souvent dans le vide : **c'est le mécanisme qui rend la panne sensible au placement (T-48)**. Le matériel le signale par `RXUNDER`, ajouté à la sonde en 0.11.2 (`5,41` index 2) avec `TXOVER` (index 3). Mesurer d'abord : si `UN` monte, la cause est tenue. Correction ensuite : attendre la file (`pio_sm_get_blocking` ou un test de `rxempty`), en vérifiant le coût sur la boucle. | **INFIRMÉ** (mesure carte bmarty 2026-09-23 : `UN` = 0 au repos et à la frappe) — la boucle ne lit jamais de file vide, la synchronisation avec le PIO est correcte et le commentaire du code dit vrai. Hypothèse close ; les compteurs restent, ils sont utiles |
| T-50 | P0 | **Durée passée loin du bus** (mesure carte 2026-09-23 : `BUS` au repos fait monter TX **et** RX, `DVI` reste à 0) : les compteurs de calage de T-49 ne distinguent pas le normal du pathologique, puisque **servir une commande API cale forcément le PIO** — le 6502 boucle sur le port de contrôle pendant que le firmware travaille. Ce qui sépare un firmware sain d'un firmware affamé est la **durée**. `5,42 Get Bus Timing` rend la plus longue `DSPSync` (P0 = 0 : clavier, tâche USB, clignotement) et la plus longue commande API (P0 = 1), en microsecondes, mesurées dans la boucle du bus par le timer matériel. Enseignement immédiat de la mesure : **`DVI` = 0, l'encodeur n'est jamais en retard** — core 1 va bien, ce qui écarte T-38 pour cette panne et confirme que les glitches du menu ne sont pas des lignes en retard. | **DONE** 0.11.1, à mesurer carte |
| T-51 | P2 | **Outils de mesure : seul Échap doit sortir** (retour carte bmarty 2026-09-23 : « late et bus s'arrêtent dès que je tape sur le clavier ») : `LATE` et `BUS` sortaient sur **n'importe quelle** touche, alors que leur raison d'être est de mesurer pendant qu'on martèle le clavier — l'en-tête de `late.asm` invitait même à le faire. Corrigé en 0.11.1 : Échap sort, les autres touches sont ignorées. | **DONE** 0.11.1 |
| T-49 | P0 | **Instrumentation du bus 6502** (demande bmarty 2026-09-23, prérequis de T-48, sans sonde : tout passe par l'écran) : la machine PIO génère l'horloge PHI2 du 6502 (side-set GPIO 21), donc un firmware en retard **cale la machine et arrête l'horloge** — le processeur est étiré, jamais nourri d'un octet faux. Le matériel garde la trace de ces calages dans `PIO_FDEBUG` (bits collants) : `TXSTALL` = la donnée d'une lecture n'était pas prête, `RXSTALL` = la file d'adresses non vidée à temps. `HWBusProbe()` (en RAM, T-46) les échantillonne depuis `DSPSync` (~95 Hz) et compte les fenêtres contenant au moins un calage ; **`5,41 Get Bus Stalls`** les rend au 6502 (P0-3 TX, P4-7 RX ; `$FF` en P0 remet à zéro) et l'outil **`BUS.NEO`** les affiche avec `5,40` (lignes DVI) pour comparer les deux famines. Coût : 0 dans la boucle du bus, 8 octets de RAM. Test `tests/api/busstall.asm` (0 sous `neo`, qui n'a pas de PIO). | **DONE** 0.11.0, à mesurer carte |
| T-48 | P0 | **La panne carte dépend du placement du code en flash — PROUVÉ** (2026-09-23, 01h40) : `trinity-0.10.12-USB.uf2` et `trinity-0.10.12b-banniere-USB.uf2` sont compilées **du même code source** et ne diffèrent que par la **chaîne de version affichée au démarrage** (la première a été construite avant le tag, d'où sa bannière `0.10.10-…`). La première échoue systématiquement, la seconde fait tourner `legdiag` **et** le jeu. Décaler quelques dizaines d'octets suffit donc à faire basculer la panne. Mécanisme le plus probable : le **cache XIP** (16 Ko) et du code critique en temps exécuté depuis la flash sur core 0 — selon leur placement, les fonctions chaudes se chassent du cache et font dépasser le budget de la boucle du bus (marge de 11 `nop`). Cohérent avec 0.10.2 (« le handler d'IRQ en flash volait des cycles à l'encodeur TMDS »), T-38 et T-31. **Conséquences** : (1) aucune conclusion de T-47 n'est valable ; (2) toute « correction » jugée sur un seul binaire est sans valeur ; (3) il faut d'abord un **protocole de mesure** — construire N binaires décalés artificiellement (padding variable) et compter combien échouent, avant et après correction. Correction visée : placer en RAM tout ce qui est dans le chemin critique de core 0, à commencer par les handlers d'IRQ USB de TinyUSB. Ancien libellé ci-dessous. | **TODO — priorité absolue** | **Mesure du 2026-09-24, qui confirme la piste (b) et n'avait jamais été faite** : dans le binaire livré, `hcd_rp2040_irq` — **le handler d'interruption USB** — se trouve à `0x1001b38c`, donc **en flash XIP**, et pèse 960 octets ; `tuh_task_ext` (832 o), `tuh_control_xfer` (436 o), `hcd_edpt_xfer` (328 o) et `f_read` (436 o) aussi. Le port RP2040 de TinyUSB 0.21 ne marque **aucune** de ses fonctions `__not_in_flash_func`. C'est donc bien du code exécuté sur core 0, en interruption, **depuis la flash**, pendant que FatFs chasse le cache XIP de 16 Ko — la configuration exacte que T-48 soupçonne. Correction à essayer : patcher la copie locale de TinyUSB (`~/neo-deps/tinyusb-0.21`) pour placer le handler et ses appelées en RAM, patch versionné dans le dépôt puisque cette dépendance ne l'est pas. Attention au protocole : mettre du code en RAM **change massivement le placement**, donc un essai isolé ne prouvera rien — il faudra les N binaires à padding variable.
| T-48b | P0 | (constat initial) **La panne dépend du binaire, pas de la logique** : `bisect-I` et 0.10.12 ont la **même taille d'UF2 (435 200 o), la même RAM, la même logique** — quelques instructions déplacées — et pourtant la première fait tourner `legdiag` et le jeu de façon stable, la seconde échoue de façon reproductible. Les neuf flashs de T-47 ont donc mesuré un **effet de placement**, pas des causes : leurs verdicts sont à relire comme des tirages. Signature d'un défaut **marginal en temps** que le placement fait basculer. Pistes à instrumenter avant tout nouveau correctif : (a) la boucle du bus (`processor_pio.cpp`) et sa marge de 11 `nop` — mesurer les transactions manquées ; (b) les **handlers d'IRQ USB de TinyUSB, en flash sur core 0** (0.10.2 a déjà constaté qu'un handler en flash volait des cycles à l'encodeur TMDS) ; (c) le cache XIP de 16 Ko et les conflits selon le placement du code chaud ; (d) pile et tas (32 544 o libres, marge mince). Règle de méthode pour la suite : **aucune image n'est déclarée saine sans plusieurs essais**, et comparer deux binaires fonctionnellement identiques est un test à part entière. | **TODO — priorité absolue** |
| T-46 | P1 | **Rien de ce qu'appelle `DSPSync` ne doit vivre en flash** (bissection carte bmarty 2026-09-23) : 0.10.3 y a ajouté `RNDCursorUpdate()`, qui est en flash et s'exécute en entier ~95 fois par seconde, alors que `DSPSync` est `TIMECRITICAL` (placée en RAM) et appelée depuis la boucle qui sert le bus du 6502. Résultat : **tout programme mourait peu après son lancement sur carte** (`legdiag`, NeoLegacy), rien sous `neo` ni Phosphoneo. Les deux autres appelées respectaient la règle : `KBDSync` est `__time_critical_func`, `CONBlinkSync` sort immédiatement sauf deux fois par seconde. 0.10.12 : plus de sondage, l'état du curseur est **publié quand il change** (`MSESetPosition`, `MSEOffsetPosition`, `MSESetVisible`, `MSEInitialise`, `CURSetCurrent`, `GFXSetMode`). Le gain visé par 0.10.3 est conservé (le callback de ligne ne touche ni code ni données en flash, avec T-44). | **DONE** 0.10.12 (faute réelle corrigée), mais **ce n'était pas la cause de la panne** : voir T-48 |
| T-47 | P1 | **Journal de la bissection du 2026-09-23** (deux régressions carte, invisibles en émulateur, trouvées en neuf flashs) : témoin = `legdiag` de NeoLegacy, plus simple et plus rapide que le jeu. 0.9.11 OK → 0.10.2 OK → 0.10.3 KO ; `bisect-D` (0.10.3 sans le sondage) OK ⇒ **cause 1 = T-46**. Puis `bisect-H` (0.10.4 sans sondage) OK, `bisect-G` (0.10.5) KO ⇒ **cause 2 = l'envoi du rapport de sortie HID de T-40**. `bisect-I` (firmware à jour, T-46 corrigé et T-40 retiré) : `legdiag` **et** le jeu passent. Leçon de méthode : `bisect-C`, construite sur 0.10.10 au lieu du commit suspect, n'a rien prouvé et a coûté un aller-retour ; et neutraliser deux choses à la fois (`bisect-E`) a brouillé le résultat. **Surtout : le contrôle final a invalidé l'ensemble** — voir T-48. Une seule variable par essai, à partir du commit exact, et plusieurs essais avant de déclarer une image saine. | **CLOS, conclusions invalidées par T-48** |
| T-17 | P2 | **Banques mémoire en XIP** (demande bmarty 2026-09-21 « je veux le XIP pour les banques ») : reprise de `1,18 Select Bank` / `1,19 Get Bank Info` (F-23) avec le **stockage des banques en flash** (XIP) et non en SRAM (le fork y mettait 2 × 8 Ko, impossible avec 35 Ko libres) : lecture seule, `Select Bank` = copie flash → fenêtre 6502 ; contenu des banques = images de 8 Ko dans une zone réservée de la flash (à définir : intégrées au firmware ou fichier `.bnk` écrit une fois). Pas d'écriture en place (programmation flash sous DVI écartée, ADR-04). **Livré 0.7.0** : 32 banques × 8 Ko en haut de la flash, `1,18`/`1,19`, **`1,22 Write Bank`** (DVI suspendu, erase + program, vérification), pages `$A0+` lecture seule, `neo` : `banks.flash`. | **DONE neo** 0.7.0, à valider carte (`trinity-0.7.0-banks-xip-USB.uf2`, `cle-usb/banks.neo6502`) |
