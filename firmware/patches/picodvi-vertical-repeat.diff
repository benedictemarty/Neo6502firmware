diff --git a/software/libdvi/dvi.c b/software/libdvi/dvi.c
index f66377f..d4c8827 100644
--- a/software/libdvi/dvi.c
+++ b/software/libdvi/dvi.c
@@ -28,6 +28,7 @@ void dvi_init(struct dvi_inst *inst, uint spinlock_tmds_queue, uint spinlock_col
 		inst->dma_cfg[i].dreq = pio_get_dreq(inst->ser_cfg.pio, inst->ser_cfg.sm_tmds[i], true);
 	}
 	inst->late_scanline_ctr = 0;
+	if (inst->vertical_repeat == 0) inst->vertical_repeat = DVI_VERTICAL_REPEAT;
 	inst->tmds_buf_release_next = NULL;
 	inst->tmds_buf_release = NULL;
 	queue_init_with_spinlock(&inst->q_tmds_valid,   sizeof(void*),  8, spinlock_tmds_queue);
@@ -201,7 +202,7 @@ static void __dvi_func(dvi_dma_irq_handler)(struct dvi_inst *inst) {
 		tmdsbuf = NULL;
 	}
 	else if (queue_try_peek_u32(&inst->q_tmds_valid, &tmdsbuf)) {
-		if (inst->timing_state.v_ctr % DVI_VERTICAL_REPEAT == DVI_VERTICAL_REPEAT - 1) {
+		if ((inst->timing_state.v_ctr & (inst->vertical_repeat - 1)) == inst->vertical_repeat - 1) {
 			queue_remove_blocking_u32(&inst->q_tmds_valid, &tmdsbuf);
 			inst->tmds_buf_release_next = tmdsbuf;
 		}
@@ -209,20 +210,21 @@ static void __dvi_func(dvi_dma_irq_handler)(struct dvi_inst *inst) {
 	else {
 		// No valid scanline was ready (generates solid red scanline)
 		tmdsbuf = NULL;
-		if (inst->timing_state.v_ctr % DVI_VERTICAL_REPEAT == DVI_VERTICAL_REPEAT - 1)
+		if ((inst->timing_state.v_ctr & (inst->vertical_repeat - 1)) == inst->vertical_repeat - 1)
 			++inst->late_scanline_ctr;
 	}
 
 	switch (inst->timing_state.v_state) {
 		case DVI_STATE_ACTIVE:
 			if (tmdsbuf) {
-				dvi_update_scanline_data_dma(inst->timing, tmdsbuf, &inst->dma_list_active);
+				if (inst->lane_offsets_valid) dvi_update_scanline_data_dma_lanes(tmdsbuf, inst->lane_word_offset, &inst->dma_list_active);   // bmarty
+				else dvi_update_scanline_data_dma(inst->timing, tmdsbuf, &inst->dma_list_active);
 				_dvi_load_dma_op(inst->dma_cfg, &inst->dma_list_active);
 			}
 			else {
 				_dvi_load_dma_op(inst->dma_cfg, &inst->dma_list_error);
 			}
-			if (inst->scanline_callback && inst->timing_state.v_ctr % DVI_VERTICAL_REPEAT == DVI_VERTICAL_REPEAT - 1) {
+			if (inst->scanline_callback && (inst->timing_state.v_ctr & (inst->vertical_repeat - 1)) == inst->vertical_repeat - 1) {
 				inst->scanline_callback();
 			}
 			break;
diff --git a/software/libdvi/dvi.h b/software/libdvi/dvi.h
index 73be66c..f143368 100644
--- a/software/libdvi/dvi.h
+++ b/software/libdvi/dvi.h
@@ -25,6 +25,13 @@ struct dvi_inst {
 	struct dvi_serialiser_cfg ser_cfg;
 	// Called in the DMA IRQ once per scanline -- careful with the run time!
 	dvi_callback_t scanline_callback;
+	// Runtime vertical repeat (1 = native lines, 2 = each scanline shown twice).
+	// 0 at dvi_init() time selects the compile time DVI_VERTICAL_REPEAT.
+	uint vertical_repeat;
+	// Runtime word offset of each lane inside a TMDS buffer (bmarty, monochrome modes : lanes may
+	// share one encoded channel and dark lanes point to a black one). Used when lane_offsets_valid.
+	uint32_t lane_word_offset[N_TMDS_LANES];
+	bool lane_offsets_valid;
 
 	// State ---
 	struct dvi_scanline_dma_list dma_list_vblank_sync;
diff --git a/software/libdvi/dvi_timing.c b/software/libdvi/dvi_timing.c
index cff8edc..5d0f1a3 100644
--- a/software/libdvi/dvi_timing.c
+++ b/software/libdvi/dvi_timing.c
@@ -342,3 +342,14 @@ void __dvi_func(dvi_update_scanline_data_dma)(const struct dvi_timing *t, const
 	}
 }
 
+
+// bmarty : per lane word offsets (monochrome modes : one encoded channel shared by the lit lanes).
+void __dvi_func(dvi_update_scanline_data_dma_lanes)(const uint32_t *tmdsbuf, const uint32_t *lane_word_offset, struct dvi_scanline_dma_list *l) {
+	for (int i = 0; i < N_TMDS_LANES; ++i) {
+		const uint32_t *lane_tmdsbuf = tmdsbuf + lane_word_offset[i];
+		if (i == TMDS_SYNC_LANE)
+			dvi_lane_from_list(l, i)[3].read_addr = lane_tmdsbuf;
+		else
+			dvi_lane_from_list(l, i)[1].read_addr = lane_tmdsbuf;
+	}
+}
diff --git a/software/libdvi/dvi_timing.h b/software/libdvi/dvi_timing.h
index 8228ac0..37b7961 100644
--- a/software/libdvi/dvi_timing.h
+++ b/software/libdvi/dvi_timing.h
@@ -96,5 +96,6 @@ void dvi_setup_scanline_for_active(const struct dvi_timing *t, const struct dvi_
 		uint32_t *tmdsbuf, struct dvi_scanline_dma_list *l);
 
 void dvi_update_scanline_data_dma(const struct dvi_timing *t, const uint32_t *tmdsbuf, struct dvi_scanline_dma_list *l);
+void dvi_update_scanline_data_dma_lanes(const uint32_t *tmdsbuf, const uint32_t *lane_word_offset, struct dvi_scanline_dma_list *l);   // bmarty
 
 #endif
